# .github/workflows/keep-alive.yml
name: Keep Alive

on:
  schedule:
    # 每月 1 号 UTC 时间 0 点运行（确保 <60 天）
    - cron: "0 0 1 * *"

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Stay active
        run: echo "This workflow keeps GitHub Actions enabled."
